console.error(`-------------------------------------------------------------------------------`);
function factorialOfWordsCount(words) {
  
 for (let index = 0; index < words.length; index++) {
    var wordscount = words.split(` `);
    var totalcount = wordscount.length;

 let factorial=1
 for (let index = totalcount; index >=1; index--) {
    var factorialTotal =factorial=factorial*index

    
 }
 }
if (words==''|| words=="") {
    console.log(`The given input is invalid `);
} else {
    console.log(`The length of ${words} is ${totalcount} and Total factorial is ${factorialTotal }`);
    console.error(`-------------------------------------------------------------------------------`);
}
}
 

 factorialOfWordsCount(`Revision is the mother of success`);
 factorialOfWordsCount(`We never fail we always learn`)
 factorialOfWordsCount(``)
 factorialOfWordsCount("")
 factorialOfWordsCount(`Arjun Anil Bute`)
 
 